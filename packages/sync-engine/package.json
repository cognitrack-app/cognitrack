{
  "name": "@cognitrack/sync-engine",
  "version": "1.0.0",
  "description": "Offline-first sync queue for CogniTrack desktop",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "@cognitrack/api-client": "workspace:*",
    "@cognitrack/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20.14.2",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
